- defaults:
    link:
      create: true
      relink: true
      force: true


- clean:
    ~/:
      force: true
    ~/.config:
      recursive: true

- link:
    ~/.config/alacritty: alacritty
    ~/.config/bat: bat
    ~/.config/git: git

# - shell:

