- defaults:
    link:
      create: true
      relink: true
      force: true


- clean:
    ~/:
      force: true
    ~/.config:
      recursive: true

- link:
    # home
    ~/.profile: profile
    ~/.zshenv: profile
    

    # .config
    ~/.config/kanshi: config/kanshi
    ~/.config/yofi: config/yofi
    ~/.config/waylogout: config/waylogout
    ~/.config/pacman: config/pacman
    ~/.config/alacritty: config/alacritty
    ~/.config/bat: config/bat
    ~/.config/wob: config/wob
    ~/.config/gammastep: config/gammastep
    ~/.config/git: config/git
    ~/.config/i3status-rust: config/i3status-rust
    ~/.config/mako: config/mako
    ~/.config/mpv: config/mpv
    ~/.config/sway: config/sway
    ~/.config/sworkstyle: config/sworkstyle
    ~/.config/zsh: config/zsh
    ~/.config/thorium-flags.conf: config/chromium-flags.conf
    ~/.config/code-flags.conf: config/chromium-flags.conf
    ~/.config/mimeapps.list: config/mimeapps.list
    ~/.config/ranger: config/ranger
    
    # .local
    ~/.local/bin: local/bin
    ~/.local/share/applications: local/share/applications

